/*
 * LoginPage Messages
 *
 * This contains all the text for the LoginPage container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.layouts.PrimaryLayout';

export default defineMessages({
  header: {
    id: `${scope}.header`,
  },
});
